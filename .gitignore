node_modules
.env
.DS_Store

# Turborepo
.turbo/

# Build artifacts
build/
**/dist/
**/.next/
logs/

# TypeScript incremental build info
**/*.tsbuildinfo

# Env files (keep examples tracked)
.env
.env.*
!.env.example

# Logs
*.log

apps/web/tmp_uploads/
.vercel
.env*.local

# Local helper scripts (machine-specific / personal wrappers at repo root)
run-nexus-dev.sh
start-dev-clear-all.sh
start-dev-clear_ALL.sh
start-dev-local.sh
start-dev-cloud-all.sh
start-prod-all.sh

# Mobile build artifacts
*.ipa
*.apk
android/
ios/

# Data promotion artifacts (never commit real data snapshots)
scripts/data-promotion/*.json
scripts/data-promotion/*.csv
promotion/
*.app
*.mov
# Office/RTF exports
*.rtf

# Local uploads / artifacts (do not commit)
/apps/api/uploads/

# Data files — reference data, CSVs, Excel, images, PDFs (not source code)
/docs/data/

# Plan sheet images (large webp files)
/docs/plan-sheets/

# Build / tool caches (should never be committed)
.cache/

# Legacy entries
/remoteLogId.
/scripts/mport.js
/{

