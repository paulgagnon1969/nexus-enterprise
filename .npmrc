# The Mac Studio runs both dev and production stacks.
# NODE_ENV=production is set globally for the shadow containers,
# which causes npm to skip devDependencies (omit=dev).
# This override ensures dev tooling (@types/*, typescript, etc.)
# always installs in this repo.
include=dev
