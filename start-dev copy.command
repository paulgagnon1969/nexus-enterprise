#!/usr/bin/env bash
# One-click dev stack starter for Nexus Enterprise.
# Double-click this file in Finder to:
# - cd into ~/nexus-enterprise
# - run ./start-dev.sh (which starts Cloud SQL proxy, API, web, worker)

cd "$HOME/nexus-enterprise"
./start-dev.sh
