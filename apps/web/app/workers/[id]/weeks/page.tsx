export const dynamic = "force-dynamic";

export default function WorkerWeeksPage() {
  return (
    <main className="p-6 space-y-4">
      <h1 className="text-2xl font-semibold">Worker weeks (legacy BIA view)</h1>
      <p className="text-sm text-gray-600">
        This legacy worker weeks view is temporarily disabled in this
        deployment. Please use the main Nexus interface for current payroll
        and worker activity.
      </p>
    </main>
  );
}
