export const dynamic = "force-dynamic";

export default function WeeksPage() {
  return (
    <main className="p-6 space-y-4">
      <h1 className="text-2xl font-semibold">Work Weeks (legacy BIA / LCP)</h1>
      <p className="text-sm text-gray-600">
        This legacy work weeks view is temporarily disabled in this deployment.
        Existing payroll and hours data are still available through the main
        Nexus interface.
      </p>
    </main>
  );
}
