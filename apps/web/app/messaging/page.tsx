"use client";

import { PageCard } from "../ui-shell";

export default function MessagingPage() {
  return (
    <PageCard>
      <h2 style={{ marginTop: 0 }}>Messaging</h2>
      <p style={{ fontSize: 13, color: "#6b7280" }}>
        Messaging features will appear here (team chat, notifications, and activity).
      </p>
      <p style={{ fontSize: 13, color: "#6b7280" }}>
        This is a temporary placeholder so the <strong>Messaging</strong> tab does not show a 404.
      </p>
    </PageCard>
  );
}
